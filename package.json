{
  "name": "kronos-cotacoes",
  "version": "7.0.0",
  "private": true,
  "description": "Boletim diário KRONOS — dólar buscado direto do navegador (sem rate limit Vercel).",
  "scripts": { "dev": "vercel dev", "deploy": "vercel --prod" },
  "engines": { "node": ">=18" }
}
